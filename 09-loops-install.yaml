- name : Installing packages through loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: Using loops 
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    loop :
      - nginx
      - mysql
      - zip
