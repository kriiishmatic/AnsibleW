- name : Installing packages through loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: Using loops 
    ansible.builtin.packages:
      name: "{{ item }}"
      state: latest
    loop :
      - nginx
      - mysql
      - zip
